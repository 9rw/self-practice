Week2 - Shallow Equality & Deep Equality check the properties values for equality.

รายละเอียด

1. Shallow Equality
Shallow(ตื้น) คือการ check แบบ top level(ชั้นบนสุด) ไม่สามารถลงไปลึกกว่านี้ได้
หมายความว่าถ้าเป็น nested object แล้วมี values เหมือนกันผลลัพธ์จะออกมาเป็น false เพราะไม่สามารถเช็คได้

# หลักการทำงาน #
1. ดึง keys ของ object มาเก็บไว้ในตัวแปรโดยใช้ Object.keys() return ออกมาเป็น array ที่เก็บ keys
2. นำตัวแปรที่เก็บ keys มาเช็ตต่าความยาว ว่าเท่ากันมั้ยถ้าไม่ ก็จะ return false
3. for-of-loop ข้างใน keys เช็คว่า value ของ object1 กับ object2 เท่ากันมั้ยถ้าไม่ return false
4. ถ้าไม่เข้าเงื่อนไขทั้งหมดก็ return true
---------------------------------------------------------------------------------------

2. Deep Equality
Deep(ลึก) คือการ check แบบ recursive(ลึกลงไปเรื่อยๆ)
หมายความว่าถ้าเป็น nested object ก็จะสามารถเช็คค่าของ object ที่ซ้อนอยู่ได้

# หลักการทำงาน #
1. ตรวจสอบก่อนว่า object ทั้งสองเป็น object จริง ๆ (ไม่ใช่ null, array, หรือ primitive)
   โดยอาจใช้ typeof และตรวจสอบว่าไม่ใช่ null
2. ถ้าไม่ใช่ object ทั้งคู่ (คือเป็น primitive เช่น string, number, boolean ฯลฯ)
   ให้เปรียบเทียบด้วย === แล้ว return true หรือ false
   เพื่อป้องกันการใช้ Object.keys() แล้วค่าที่เอาไปใส่เป็น primitive ซึ่งจะทำให้ code error เพราะไม่ใช่ object
3. ถ้าเป็น array หรือ object ให้ดึง keys ของทั้งสอง object มาใช้เปรียบเทียบ
   เช็คว่า keys มีจำนวนเท่ากันไหม ถ้าไม่เท่ากัน return false
   💡 กรณีที่เราใช้ Object.keys() กับ array เราจะได้ keys ออกมาเป็น index ของ array
4. ใช้ for-loop หรือ for-of-loop วนใน keys
   > ทั้ง object1 และ object2 มี key นั้นไหม
   > แล้วเช็คว่า value ของ key นั้นทั้งสองฝั่ง มีค่าเท่ากันหรือไม่
   >> ถ้า value เป็น object ให้เรียกฟังก์ชัน deepEqual ซ้ำ (recursion)
   >> ถ้าเป็นค่า primitive เปรียบเทียบด้วย ===
5. ถ้าทุก key ผ่านการเช็คทั้งหมด → return true
   ถ้ามี key ไหนไม่เท่ากันเลยสักตัว → return false
---------------------------------------------------------------------------------------

Reflect
- Shallow Equality
- เปรียบเทียบแค่ระดับผิวเผิน (top-level)
- ถ้าเจอ nested object ที่ค่าข้างในเหมือนกัน → จะยังถือว่า ไม่เท่ากัน
- ใช้ Object.keys() และ loop เช็คค่าทีละ key

- Deep Equality
- เปรียบเทียบแบบลึก (recursive) → ตรวจค่าข้างในได้ทุกระดับ
- ต้องเช็คว่าเป็น object จริง ไม่ใช่ null หรือ primitive ก่อน
- ใช้การวน loop และเรียกฟังก์ชันตัวเองซ้ำ (recursion) เพื่อเช็ค object ที่ซ้อนอยู่

- เข้าใจความแตกต่างระหว่างการเทียบค่าแบบตื้น กับแบบลึก
- Shallow = เร็วแต่เช็คค่าของ nested object ไม่ได้
- Deep = เช็คค่า nested object ได้ และใช้ทรัพยากรมากกว่า